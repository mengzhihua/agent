# Changelog

## 0.35.0

- `agent permissions` 列出记住的允许；`agent permissions clear` 清空 `~/.agent/permissions.json`
- 交互 `/permissions` 查看同一份列表
- GitHub Release 说明带上本文件里对应版本的变更
- README 补上 Windows arm64 安装包

## 0.34.0

- `approvalMode: edits`：改文件自动放行，shell 和网络仍要确认
- 权限提示可回 `y`、`session`、`always`
